{% extends 'base.twig' %}

{% block content %}

    <h2>Edit profile</h2>

    <form action="" method="POST">
        <div id="flasherror">{{ flash.error }}</div>

        <label>Full name:</label>
        <input type = "text" name="fullname" value= "{{ user.fullname }}"> <br />

        <label>Address:</label>
        <input type = "text" name="address" value= "{{ user.address }}"> <br />

        <label>Postcode:</label>
        <input type = "number" name="postcode"  value= "{{ user.postcode }}"> <br />

        <label>Email:</label>

      
        <input type="email" name="email" value="{{ user.email}}"> <br />


        <label>Bio:</label>
        <textarea name="bio">{{ user.bio }}</textarea>

        <label>Age:</label>
        <input type="number" name="age" value="{{ user.age }}"> <br />

        <label>Bank Number:</label>
        <input type="text" name="banknumber" value="{{ user.banknumber }}"> <br />

        <input type="submit" name="submit" value="Save profile">
    </form>
{% endblock %}
